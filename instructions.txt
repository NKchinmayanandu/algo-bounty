Overall Architecture (All 3 Must Follow)

This is the shared architecture all 3 prompts must follow:

Frontend (Next.js)
        ↓
Backend API (FastAPI)
        ↓
Database (Postgres)
        ↓
Algorand Smart Contract

Important rule:

Frontend → Backend only
Backend → Blockchain only
Frontend never directly controls blockchain logic
👨‍🎨 PROMPT 1 — FRONTEND AGENT

Give this exactly to frontend AI agent:

FRONTEND AGENT PROMPT

Build the frontend for a hackathon platform where users can create tasks funded by Algorand escrow and other users can complete those tasks and automatically receive payment.

Tech Stack

Use:

Next.js (App Router)
TypeScript
TailwindCSS
Framer Motion
React Three Fiber / Drei
WebSockets
Zustand (state management)
Architecture

The frontend communicates only with:

FastAPI backend
Wallet provider (for signing only)

Frontend must NOT:

Access database directly
Control smart contract logic
Do verification
Global Layout

Persistent layout:

Top Navbar
Left Sidebar
Main Content
Footer

Navbar

Contains:

Logo
Website name
Dark mode toggle
Login/Register
Profile dropdown
Sidebar

Navigation:

Create Task
View Live Tasks
History
About
Pages
Landing Page (Public)
Hero section
3D interactive animation
Product explanation
How it works
About Page (Public)
How platform works
Escrow explanation
Rating system
FAQ
Tasks Page

Route:

/tasks

Logic:

Not logged in:

Show login prompt

Logged in but wallet not connected:

Show "Connect Wallet"

Wallet connected:

Show grid of tasks

Task Card:

Title
Description
Reward
Deadline
Creator username
Creator rating
Accept button

Sorting:

Latest
Highest pay
Lowest pay
Deadline
Create Task Page

Route:

/create-task

Fields:

Title
Description
Reward
Deadline
Optional GitHub requirement

Submit flow:

Frontend → Backend → Wallet signing

History Page

Show:

Tasks created
Tasks completed
Status
Payments
Ratings
WebSocket

Use WebSocket:

/ws/tasks

Update tasks live.

Folder Structure
apps/web/
app/
layout.tsx
page.tsx
about/
login/
register/
dashboard/
create-task/
tasks/
history/
profile/

components/
Navbar.tsx
Sidebar.tsx
TaskCard.tsx
TaskGrid.tsx
WalletConnect.tsx
Footer.tsx
Hero3D.tsx

lib/
api.ts
websocket.ts
auth.ts

hooks/
useAuth.ts
useWallet.ts
useTasks.ts
API Calls

Use these endpoints:

POST /auth/register
POST /auth/login
GET /tasks
POST /tasks
POST /tasks/{id}/claim
POST /tasks/{id}/submit
GET /users/{id}
GET /history
State Management

Use Zustand:

Auth
Wallet
Tasks
Theme
UI Requirements
Apple-style UI
Smooth animations
Dark mode
Rounded cards
Clean typography
Backend URL

Use:

NEXT_PUBLIC_API_URL=http://localhost:8000
👨‍💻 PROMPT 2 — BACKEND AGENT

Give this to backend AI agent:

BACKEND AGENT PROMPT

Build the backend API for a trustless task bounty platform using FastAPI.

Tech Stack

Use:

FastAPI
PostgreSQL
SQLAlchemy
Alembic
WebSockets
JWT Authentication
Database Models

Users

id
username
password_hash
rating_avg
rating_count
created_at

Wallets

user_id
wallet_address

Tasks

id
title
description
reward
status
creator_user_id
assignee_user_id
created_at

Submissions

task_id
repo_url
report
verification_status

Ratings

task_id
rater
rated
stars
Task States
OPEN
FUNDED
CLAIMED
SUBMITTED
VERIFIED
PAID
API Endpoints

Auth:

POST /auth/register
POST /auth/login
GET /auth/me

Tasks:

GET /tasks
POST /tasks
GET /tasks/{id}
POST /tasks/{id}/claim
POST /tasks/{id}/submit

Users:

GET /users/{id}
GET /history
WebSocket
/ws/tasks

Send updates when:

Task created
Task claimed
Task completed
GitHub Verification

Check:

repo exists
public
README exists

Use GitHub API.

Blockchain Integration

Backend must:

Create escrow transaction
Assign contributor
Release payment

Use:

algosdk

Backend calls blockchain service:

create_escrow()
assign_worker()
release_payment()
Folder Structure
apps/api/
app/
main.py
models/
schemas/
routes/
services/
db/
realtime/
Env Variables
DATABASE_URL=
JWT_SECRET=
ALGORAND_NODE_URL=
ALGORAND_TOKEN=
GITHUB_TOKEN=
🔗 PROMPT 3 — BLOCKCHAIN AGENT

Give this to blockchain AI agent:

BLOCKCHAIN AGENT PROMPT

Build an Algorand smart contract escrow system.

Tech Stack

Use:

Algorand
PyTeal
Beaker
AlgoSDK
Contract Responsibilities
Lock funds
Assign contributor
Release payment
Contract States
OPEN
FUNDED
CLAIMED
SUBMITTED
VERIFIED
PAID
Functions

create_task

creator
reward

assign_worker

task_id
worker

release_payment

task_id
Deployment Script

Create:

deploy.py

Deploy to:

Algorand TestNet

Folder Structure
contracts/
escrow/
contract.py
deploy.py
Integration

Backend calls:

create_task()
assign_worker()
release_payment()
🧪 HOW TO JOIN ALL THREE

After all three AI agents generate code:

Folder:

repo/
apps/web
apps/api
contracts
Run Backend
cd apps/api
uvicorn app.main:app --reload

Backend:

localhost:8000
Run Frontend
cd apps/web
npm run dev

Frontend:

localhost:3000
Deploy Smart Contract
cd contracts
python deploy.py
🧪 TEST FLOW

Step 1

Open:

localhost:3000

Step 2

Register user

Step 3

Create task

Step 4

Wallet connect

Step 5

Another user claims

Step 6

Submit repo

Step 7

Backend verifies

Step 8

Smart contract payout

if at all anything extra Is required (like wallet address or other stuff,ask me to provide,make sure u understand it fully before making,make it setp by step)
it should be deployable on web
.env file also must